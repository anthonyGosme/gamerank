{
  "name": "@gamerank/sdk",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build": "esbuild src/index.ts --bundle --minify --format=iife --target=es2019 --outfile=dist/sdk.js"
  },
  "devDependencies": {
    "esbuild": "^0.24.2",
    "typescript": "^5.7.2"
  }
}
